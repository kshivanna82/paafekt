// AndroidCameraCapture.cpp
#include "PlatformCameraCaptureBridge.h"

// If/when you add Android camera2, call the registered callback with FISPixelBufferRef (void* here).
// For now, keep stubs so iOS can build without Android bits breaking.
